HHF
===

This is the code repo for the deck Hatched Horses Fighting. 

Before running any scripts, the latest decklist and tags must be in `deck.txt` (just the 99) and `tagged.json` (the output of running `coptags.js` with the decklist open to view by tags).